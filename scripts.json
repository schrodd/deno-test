{
  "$schema": "https://deno.land/x/denon@2.5.0/schema.json",
  "scripts": {
    "start": {
      "cmd": "deno run app.ts",
      "desc": "run my app.ts file"
    },
    "fetch": {
      "cmd": "deno run --allow-net fetch.ts",
      "desc": "run fetch test file"
    }
  }
}